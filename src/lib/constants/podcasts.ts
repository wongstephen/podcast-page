import { sortByOptionType } from '../types/podcast';

export const dropdownOptionsMap: Record<sortByOptionType, string> = {
  newest: 'Newest',
  oldest: 'Oldest',
  title: 'Title',
};

export const maxCardLength = 4;
